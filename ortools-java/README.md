# OR-Tools Java Track

This track mirrors the Python OR-Tools knapsack model in Java.

It uses a portable Maven toolchain from the workspace and writes the same CSV-style output contract as the other tracks.
